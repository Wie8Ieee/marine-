# ملخص مراجعة مشروع كشف النفايات البحرية

## الهدف من المراجعة

تهدف هذه المراجعة إلى التحقق من سلامة المقارنة بين النماذج الثلاثة:

- YOLOv8s
- Faster R-CNN ResNet-50-FPN
- SSDLite MobileNetV3-Large

والتأكد من أن النتائج قابلة لإعادة الإنتاج ويمكن الدفاع عنها علمياً في الورقة البحثية.

## المشكلة المكتشفة في التجربة الأولى

استخدمت التجربة الأولى تقسيماً عشوائياً للصور:

```yaml
split_mode: stratified_70_15_15
```

لكن صور Trash-ICRA19 مأخوذة من تسلسلات فيديو، ولذلك تكون الإطارات المتجاورة شديدة التشابه من حيث الجسم والخلفية والإضاءة وزاوية التصوير.

كشف التدقيق أن **170 من أصل 175 تسلسل تصوير ظهرت في أكثر من قسم واحد**. كان من الممكن، مثلاً، تدريب النموذج على إطار من تسلسل معين ثم اختباره على إطار قريب جداً من التسلسل نفسه.

يُعد ذلك تسرباً للبيانات (Data Leakage)، ويؤدي إلى تقدير متفائل لقدرة النموذج على التعميم.

النتائج القديمة ليست مختلقة؛ فهي صحيحة بالنسبة إلى التشغيل الذي أنتجها، لكنها لا تمثل اختباراً مستقلاً على تسلسلات تصوير غير مرئية.

## التصحيحات المنفذة

### 1. التقسيم على مستوى التسلسل

تم استبدال تقسيم الصور المستقل بتقسيم التسلسلات الكاملة:

```yaml
split_mode: sequence_70_15_15
split_seed: 42
```

يوضع كل تسلسل بالكامل في التدريب أو التحقق أو الاختبار. أظهر التدقيق وجود:

- 125 تسلسل تدريب
- 25 تسلسل تحقق
- 25 تسلسل اختبار

ولا يوجد تداخل معروف بينها.

### 2. تدقيق هوية التسلسل

أضيف استخراج هوية التسلسل من اسم الصورة، مع ملفات تدقيق توثق الصورة والتسلسل والقسم، ومنها:

```text
sequence_regex_audit.csv
sequence_split_audit.csv
```

كما أصبح البرنامج يرفض الرجوع تلقائياً إلى image-level split إذا فشل التعرف على التسلسلات.

### 3. فحص الصور المتطابقة

أضيف فحص exact duplicates قبل التقسيم:

```yaml
leakage:
  deduplicate: true
```

ملف `deduplication_report.csv` لا يحتوي على سجلات محذوفة، ما يعني عدم اكتشاف صور متطابقة تماماً في البيانات التي تم فحصها. ومع ذلك، يبقى التقسيم حسب التسلسل ضرورياً لأن الإطارات المتتابعة قد تكون شديدة التشابه دون أن تكون نسخاً متطابقة.

### 4. توحيد بروتوكول المقارنة

أصبحت المقارنة تستخدم قدر الإمكان:

- التقسيم نفسه لجميع النماذج.
- ميزانية epochs نفسها.
- عتبة ثقة موحدة.
- عتبة IoU موحدة.
- Validation لاختيار أفضل checkpoint.
- Test للتقييم النهائي فقط.

الإعداد المستهدف:

```yaml
thresholds:
  confidence: 0.25
  iou_match: 0.50
  class_agnostic_nms_iou: 0.50
```

أما التجربة القديمة فاستخدمت confidence بقيمة 0.25 لـYOLO و0.50 لنماذج TorchVision، وبالتالي لم تكن Precision وRecall وF1 مقاسة عند نقطة تشغيل موحدة.

### 5. ضبط Data Augmentation

الإعداد النهائي المقترح يطبق horizontal flip فقط:

```yaml
augmentation:
  enabled: true
  horizontal_flip: 0.50
  brightness: 0.00
  saturation: 0.00
```

تم تعطيل Mosaic وMixUp وCopy-Paste والتعديلات الهندسية واللونية الخاصة بنموذج واحد، لأن تطبيقها يختلف بين Ultralytics وPIL ولا يضمن مقارنة مضبوطة.

تشغيل seed 123 الموجود استخدم brightness وsaturation بقيمة 0.2، ولذلك لا يُعد مطابقاً تماماً للإعداد النهائي الحالي ولا ينبغي دمجه إحصائياً معه.

### 6. اختيار أفضل Checkpoint

أصبح اختيار checkpoint الأفضل مبنياً على Validation mAP@0.5:0.95، لأنه يقيس جودة التوطين عبر عدة عتبات IoU، بدلاً من الاعتماد على mAP@0.5 فقط.

### 7. دعم استكمال التدريب

يدعم البرنامج استكمال YOLO من `last.pt`. كما تحفظ نماذج TorchVision حالة النموذج وoptimizer وscheduler وAMP scaler ورقم epoch، لتجنب فقدان حالة التدريب عند استكمال التشغيل.

### 8. تصحيح تقييم River

ملخص River القديم احتوى على 1,416 صورة وصفر أجسام معلّمة، ولذلك لا يمثل تقييماً كاملاً صالحاً للمقارنة.

الإعداد الجديد يحتوي على:

- 2,400 صورة
- 3,510 أجسام

تُدمج فئات River في فئة واحدة باسم `trash`، ويصبح الاختبار class-agnostic localization دون إعادة تدريب. كما يُطبق class-agnostic NMS بعد دمج الفئات.

لذلك لا تجوز مقارنة نتيجة River القديمة بالجديدة مباشرة، لأن البيانات والـannotations وطريقة القياس تغيرت.

### 9. توحيد قياس الكفاءة

تم ضبط بروتوكول FPS ليستخدم:

- Batch size يساوي 1.
- 20 صورة warm-up.
- حتى 120 صورة مقاسة.
- نقل البيانات من CPU إلى GPU.
- Forward pass.
- Post-processing وNMS.
- CUDA synchronization.

كما فُصل بين حجم tensors الخاصة بالنموذج (`model_size_mb`) وحجم ملف checkpoint (`checkpoint_size_mb`)، مع تسجيل عدد parameters وPeak GPU memory وGFLOPs عند توفرها.

## توزيع البيانات

### التقسيم القديم المتداخل

| القسم | الصور | الأجسام | Plastic | Bio | ROV |
|---|---:|---:|---:|---:|---:|
| Train | 5,378 | 7,691 | 4,460 | 1,673 | 1,558 |
| Validation | 1,153 | 1,708 | 981 | 351 | 376 |
| Test | 1,153 | 1,662 | 929 | 393 | 340 |

### التقسيم Sequence-safe الموجود حالياً

| القسم | الصور | الأجسام | Plastic | Bio | ROV |
|---|---:|---:|---:|---:|---:|
| Train | 5,138 | 7,542 | 4,500 | 1,394 | 1,648 |
| Validation | 1,253 | 1,714 | 916 | 481 | 317 |
| Test | 1,293 | 1,805 | 954 | 542 | 309 |

اختلاف النسب الدقيقة متوقع لأن وحدة التقسيم أصبحت تسلسلاً كاملاً، وليس صورة منفردة.

## النتائج القديمة

| النموذج | Precision | Recall | F1 | mAP@0.5 | mAP@0.5:0.95 |
|---|---:|---:|---:|---:|---:|
| YOLOv8s | 85.86% | 87.30% | 86.58% | 88.85% | 68.35% |
| Faster R-CNN | 89.41% | 98.01% | 93.51% | 97.60% | 75.21% |
| MobileNet SSD | 93.49% | 90.73% | 92.09% | 94.55% | 69.71% |

هذه النتائج مرتبطة بالتقسيم القديم الذي احتوى على تداخل واسع بين تسلسلات التدريب والاختبار، ولذلك لا ينبغي اعتمادها كدليل نهائي في الورقة.

## النتائج الجديدة المتوفرة

المتوفر حالياً هو تشغيل YOLOv8s ببذرة 123 على التقسيم الآمن.

### Trash-ICRA19 Test

| المقياس | النتيجة |
|---|---:|
| Precision | 63.70% |
| Recall | 69.42% |
| F1 | 66.44% |
| mAP@0.5 | 67.18% |
| mAP@0.5:0.95 | 48.98% |
| TP | 1,253 |
| FP | 714 |
| FN | 552 |
| FPS | 53.26 |
| ms/frame | 18.78 |
| Parameters | 11,126,745 |

### River Cross-domain

| المقياس | النتيجة |
|---|---:|
| Precision | 20.37% |
| Recall | 18.21% |
| F1 | 19.23% |
| mAP@0.5 | 7.74% |
| mAP@0.5:0.95 | 3.32% |
| TP | 639 |
| FP | 2,498 |
| FN | 2,871 |
| FPS | 47.08 |

تشير نتيجة River إلى domain shift كبير بين البيئة البحرية في Trash-ICRA19 وبيئة River Floating Trash.

## تفسير انخفاض النتائج

| البروتوكول | YOLO mAP@0.5 | YOLO mAP@0.5:0.95 |
|---|---:|---:|
| التقسيم القديم | 88.85% | 68.35% |
| Sequence-safe seed 123 | 67.18% | 48.98% |
| الفرق | −21.68 نقطة | −19.37 نقطة |

التفسير الأقوى هو إزالة تسرب التسلسلات. في التقسيم القديم اختُبر النموذج غالباً على إطارات شبيهة جداً بما شاهده أثناء التدريب، بينما يتطلب الاختبار الجديد التعميم على تسلسلات غير مرئية.

يمكن للبذرة وتغير توزيع الفئات والـaugmentation أن تؤثر في النتيجة، لكنها لا تفسر غالباً هذا الفرق الكبير وحدها.

النتائج الجديدة لا تعني بالضرورة أن النموذج أصبح أسوأ؛ بل تعني أن الاختبار أصبح أصعب وأكثر صدقاً في قياس التعميم.

## الاختبارات والتحقق

تم فحص:

- استخراج هوية التسلسل من أسماء الصور.
- توزيع التسلسلات على الأقسام.
- عدم مشاركة التسلسل نفسه بين الأقسام.
- الصور المتطابقة.
- أعداد الصور والأجسام والفئات.
- ملفات الإعداد والنتائج المستخدمة.
- Python syntax للبرنامج الرئيسي وأدوات توليد الرسومات.

اجتاز الكود فحص syntax. لم تكتمل الاختبارات الآلية في البيئة المحلية الحالية بسبب غياب `torch` و`pytest`. حدث الفشل أثناء استيراد PyTorch، وليس نتيجة assertion فاشلة في منطق التقسيم؛ لذلك يجب إعادة الاختبارات داخل بيئة تحتوي على dependencies المطلوبة.

## انعكاس التصحيح على الورقة

### Abstract

يجب تعليق الادعاءات الرقمية القديمة والادعاء بأن YOLO يقدم أفضل توازن نهائي إلى حين اكتمال المقارنة الجديدة. لا ينبغي اعتماد نتيجة seed 123 وحدها كنتيجة نهائية.

### Methodology

يجب توضيح أن التقسيم تم على مستوى تسلسلات الالتقاط لمنع تسرب الإطارات المرتبطة زمنياً، مع ذكر seed وأعداد التسلسلات وفحص التكرار واستخدام Validation لاختيار checkpoint وTest للتقييم النهائي.

صياغة مقترحة:

> The dataset was partitioned at the capture-sequence level. All frames belonging to a sequence were assigned exclusively to the training, validation, or test partition to prevent temporal and scene-level leakage.

### Augmentation

يجب وصف horizontal flip الموحد فقط إذا استُخدم الإعداد النهائي، وحذف الادعاء بتطبيق Mosaic أو HSV augmentation ضمن المقارنة المضبوطة.

### جداول النتائج

لا ينبغي عرض الجدول القديم بوصفه النتيجة النهائية. يجب انتظار نتائج seed 42 للنماذج الثلاثة على التقسيم نفسه. ولا يجوز عرض mean أوstandard deviation أوconfidence intervals قبل وجود تشغيلات مستقلة مكتملة لكل نموذج تحت البروتوكول نفسه.

### الرسومات والأمثلة النوعية

ينبغي إعادة توليد منحنيات التدريب وspeed–accuracy plot والمقارنة النوعية وأمثلة FP/FN من checkpoints الخاصة بالتجربة الجديدة. الرسومات الحالية مرتبطة بنتائج قديمة أو seed 123 ولا تمثل المقارنة النهائية.

### Cross-domain

إما أن تُقيّم النماذج الثلاثة الجديدة على River بالكامل، أو يُحذف جدول River والادعاءات المتعلقة به. لا يجوز دمج نتيجة River القديمة مع نتائج in-domain الجديدة.

### Results and Discussion

يجب تفسير الفرق بين التقسيمين بوصفه دليلاً على أثر الارتباط الزمني وتسرب المشاهد. كما يمكن مناقشة ضعف River بوصفه domain shift بعد اكتمال تقييم النماذج الثلاثة.

### Conclusion

لا يمكن تثبيت النموذج الأفضل للدقة أو النشر أو التوازن قبل اكتمال التجربة الجديدة. يمكن حالياً اعتماد استنتاج منهجي مؤقت:

> Sequence-independent evaluation produced substantially lower performance than image-level random splitting, demonstrating that temporally correlated frames can inflate reported detection accuracy.

## الوضع العلمي الحالي

تنقسم النتائج الموجودة إلى ثلاث فئات:

1. نتائج قديمة مكتملة للنماذج الثلاثة، لكنها متأثرة بتداخل التسلسلات.
2. نتيجة YOLO seed 123 أكثر صرامة، لكنها ليست مطابقة تماماً للإعداد النهائي وليست مقارنة مكتملة.
3. تجربة نهائية مطلوبة للنماذج الثلاثة على sequence-safe split ولم تكتمل بعد.

يجب عدم دمج هذه الفئات في جدول واحد أو تقديمها على أنها تجربة واحدة.

## الخطوات التالية

1. تثبيت مسارات البيانات الصحيحة في RunPod.
2. استخدام البيانات sequence-safe والإعداد الموحد مع brightness وsaturation بقيمة صفر.
3. تشغيل seed 42 من initialization نظيف للنماذج الثلاثة.
4. تقييم النماذج الثلاثة على Trash-ICRA19 وRiver.
5. أرشفة الإعداد وملخص البيانات وتقارير deduplication وsequence audit والنتائج والـcheckpoints.
6. إعادة توليد جميع جداول وأشكال الورقة من النتائج الجديدة فقط.
7. تشغيل بذرتين إضافيتين، إذا توفرت موارد الحوسبة، للحصول على mean وstandard deviation وconfidence intervals.
8. تحديث Abstract وResults وDiscussion وConclusion وفق النتائج النهائية.

## الخلاصة

تم تنفيذ تصحيح جوهري لمنهجية التقييم، وليس مجرد تغيير لإعدادات التدريب. الانخفاض الحالي متوقع لأن الاختبار الجديد أزال التشابه المتسرب بين التدريب والاختبار وأصبح يقيس التعميم على تسلسلات جديدة.

نتيجة YOLO الجديدة أكثر واقعية من النتيجة القديمة، لكنها ما زالت نتيجة وسيطة. لا تصبح بديلاً نهائياً لجداول الورقة إلا بعد تشغيل النماذج الثلاثة تحت البروتوكول sequence-safe نفسه وإعادة توليد الجداول والرسومات منها.
